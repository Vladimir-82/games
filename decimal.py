def decimal(some_list, number):
    i=0
    counter=0
    while i<=len(some_list):
        res*=(some_list[i]/some_list[i+1])

            i += 1
        return res
some_list=[1,2,3,4]
number=100

print(decimal(some_list, number))